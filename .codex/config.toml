# Developer-session defaults only. QED run policy lives in its typed application
# config so model capabilities, budgets, sandboxes, and provenance have one owner.

[features]
multi_agent = true
hooks = true

[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
